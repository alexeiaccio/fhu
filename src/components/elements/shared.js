/* global tw */
import styled from '@emotion/styled'

export const MenuItemContent = styled.span`
  ${tw([
    'flex-grow',
    'flex-no-shrink',
    'p-q24',
    'truncate',
    'whitespace-no-wrap',
  ])};
`
